const app = require("./server.ts");


describe('Start tests', () => {
    
    // create a test that expects true to be true
    test('It should return true', () => {
        expect(true).toBe(true);
    });
    

});

